global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: 'gofr-pprof-demo'
    metrics_path: '/metrics'
    static_configs:
      - targets: ['host.docker.internal:2121']
        labels:
          app_name: 'pprof-demo'
